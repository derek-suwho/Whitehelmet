---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - ROLE-01
  - ROLE-02

must_haves:
  truths:
    - "Server starts and serves static files and API routes from same process"
    - "All Phase 1 success criteria have been manually confirmed by the user"
  artifacts:
    - path: "Whitehelmet/serve.mjs"
      provides: "Working API server (Plans 01-01)"
    - path: "Whitehelmet/index.html"
      provides: "UI with role switcher (Plan 01-02)"
  key_links:
    - from: "Browser"
      to: "http://localhost:3000"
      via: "serve.mjs serving index.html + API routes"
---

<objective>
Human verification checkpoint for Phase 1 Foundation. Confirms the complete foundation works end-to-end: server starts, API routes work, UI loads without errors, role switcher is functional, and each role shows the correct panel configuration.

Purpose: Gate before Phase 2 (Template Management) begins. Ensures INFRA-01 through ROLE-02 are all working.
Output: Human-approved Phase 1 baseline.
</objective>

<execution_context>
@/Users/artemd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/artemd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01-01: serve.mjs extended with API routing layer — GET/POST /api/templates, GET/POST /api/reports, GET /api/health, JSON file persistence to data/ directory, /data/ path blocked from static serving.

    Plan 01-02: index.html extended with role switcher select element in the top-bar and CSS [data-role] attribute rules controlling panel visibility for four roles (Contractor, Project Manager, System Admin, Project Director).
  </what-built>
  <how-to-verify>
    **Step 1 — Start the server:**
    ```bash
    cd /Users/artemd/Desktop/whitehelmet/Whitehelmet && node serve.mjs
    ```
    Expected: "Whitehelmet dev server running at http://localhost:3000"

    **Step 2 — Verify API (run in a new terminal):**
    ```bash
    # Health check
    curl -s http://localhost:3000/api/health
    # Expected: {"status":"ok"}

    # POST a template
    curl -s -X POST -H 'Content-Type: application/json' \
      -d '{"name":"Daily Report","description":"Contractor daily report"}' \
      http://localhost:3000/api/templates
    # Expected: {"id":"<uuid>"}

    # Verify file on disk
    ls /Users/artemd/Desktop/whitehelmet/Whitehelmet/data/templates/
    # Expected: one .json file

    # GET templates
    curl -s http://localhost:3000/api/templates
    # Expected: [{"id":"...","name":"Daily Report","description":"..."}]

    # Block check
    curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/data/templates/anything.json
    # Expected: 403
    ```

    **Step 3 — Verify UI (in browser at http://localhost:3000):**
    - [ ] No errors in browser DevTools Console tab
    - [ ] All three panels visible: Sources (left), Excel Editor (center), AI Chat (right)
    - [ ] Role switcher select element visible in the top-right header area
    - [ ] Select "Project Manager" → Sources panel disappears
    - [ ] Select "System Admin" → AI Chat panel disappears
    - [ ] Select "Project Director" → both Sources and AI Chat disappear, only Excel Editor remains
    - [ ] Select "Contractor" → all three panels return
    - [ ] Chat still works: type a message, press Enter, mock reply appears
  </how-to-verify>
  <resume-signal>
    Type "approved" to continue to Phase 2, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
All checklist items in the checkpoint task above pass human review.
</verification>

<success_criteria>
User types "approved" — confirming:
1. API server handles template storage (INFRA-01 / INFRA-02)
2. Three-panel layout intact (INFRA-03)
3. Role switcher visible and functional (ROLE-01)
4. Each role shows a distinct appropriate view (ROLE-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` following the summary template. Then update STATE.md: Phase 1 complete, ready for Phase 2.
</output>
