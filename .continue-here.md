---
phase: group2-implementation
task: login-setup
status: blocked
last_updated: 2026-04-22T21:07:13.259Z
---

<current_state>
Group 2 (PM Dashboard & Consolidation) is fully built and TypeScript-clean. The dev server is running at http://localhost:5173. The user is trying to log in at /login with dsu87654321@gmail.com / 1234 but getting "Invalid login credentials" because there are zero users in Supabase Auth (auth.users). The old FastAPI public.users table has the account but Supabase Auth is a completely separate system.

Two blockers remain before the admin dashboard is usable:
1. No Supabase Auth account for dsu87654321@gmail.com — needs to be created via admin API
2. The migration SQL (new tables: profiles, organizations, templates, etc.) has never been run against the Supabase project — only the old FastAPI tables exist
</current_state>

<completed_work>

- Installed @supabase/supabase-js in frontend
- Created frontend/.env.example and frontend/.env.local (with real Supabase credentials from root .env)
- Created frontend/src/lib/supabase.ts — typed Supabase client
- Created frontend/src/types/database.ts — full schema types for all 8 tables
- Rewrote frontend/src/stores/auth.ts — now uses Supabase Auth (signInWithPassword, onAuthStateChange, profile fetch)
- Updated frontend/src/router/index.ts — added role guard + all 3 group route blocks + /admin nested routes
- Created supabase/migrations/20260422130000_g2_templates.sql — full schema (8 tables) + baseline RLS + Group 2 RLS
- Created all 4 Edge Functions: g2-parse-template, g2-generate-template-ai, g2-consolidate, g2-finetune-consolidated
- Created frontend/src/stores/templates.ts and admin.ts
- Created 7 template components: TemplateStatusBadge, FieldTypeSelect, ValidationRuleBuilder, ColumnRow, ColumnEditor, VersionHistoryTable, AIChatPanel
- Created AdminLayout.vue + 6 admin views + 3 modals
- Fixed all TypeScript errors (npm run type-check passes clean)
- Dev server running at http://localhost:5173
</completed_work>

<remaining_work>

- Create Supabase Auth user for dsu87654321@gmail.com with password 1234:
  ```bash
  curl -X POST "https://zzxwunnnrttzuxejjwwv.supabase.co/auth/v1/admin/users" \
    -H "apikey: [service_role_key from root .env]" \
    -H "Authorization: Bearer [service_role_key]" \
    -H "Content-Type: application/json" \
    -d '{"email":"dsu87654321@gmail.com","password":"1234","email_confirm":true}'
  ```
- Run the migration SQL against Supabase (paste supabase/migrations/20260422130000_g2_templates.sql into Supabase dashboard → SQL Editor)
- After migration: create profiles row for the new auth user with role='pif_admin'
- After migration + auth user + profile: test /admin/templates in browser
- Create a PIF organization row in organizations table
- Set up storage buckets in Supabase dashboard: templates, submissions, consolidated
- Set ANTHROPIC_API_KEY + SUPABASE_SERVICE_ROLE_KEY as Edge Function secrets for the 4 g2-* functions
</remaining_work>

<decisions_made>

- Group 2 uses Supabase Auth (not old FastAPI) — this is a deliberate architecture change; old FastAPI login is for the legacy workspace only
- Hand-written database.ts types (not generated) — temporary until supabase gen types typescript can be run against the live project; update() calls use (supabase as any) casts to work around the limitation
- Dev server runs from /Users/kristenfu/frontend via npm run dev
- Supabase project ref: zzxwunnnrttzuxejjwwv (credentials already in root .env and frontend/.env.local)
</decisions_made>

<blockers>

- No Supabase Auth users exist yet — user rejected the curl command to create one (may have been accidental rejection)
- Migration SQL not yet applied — profiles/organizations/templates tables don't exist in Supabase
</blockers>

<context>
The Group 2 codebase is complete and correct. The only thing blocking a working demo is the Supabase setup (auth user + migration). The dev server is already running. Once the migration runs and the auth user is created with a profiles row (role=pif_admin), navigating to /admin/templates should show the full PM dashboard.

The old FastAPI workspace (/) still works as before — those routes use the old API via the Vite proxy to port 8000.
</context>

<next_action>
Start with: Run the curl command to create the Supabase Auth user, then run the migration SQL in Supabase dashboard, then create the profiles row. Dev server is already running at http://localhost:5173.

Key credentials in /Users/kristenfu/.env:
- SUPABASE_URL=https://zzxwunnnrttzuxejjwwv.supabase.co
- SUPABASE_SERVICE_ROLE_KEY=[in root .env]
</next_action>
